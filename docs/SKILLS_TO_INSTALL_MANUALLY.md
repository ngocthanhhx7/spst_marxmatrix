Tài liệu này chỉ là shortlist và hướng dẫn tham khảo. Agent chưa cài, chưa tải,
chưa copy và chưa kích hoạt bất kỳ skill hoặc MCP nào.

# Skills and MCP audit

No skill or MCP has been installed, copied, downloaded, or activated for MarxMatrix. `gsap-skills/` was already untracked user content and remains preserved outside the product scope.

| Item                     | Status                   | Use boundary                                                                                        |
| ------------------------ | ------------------------ | --------------------------------------------------------------------------------------------------- |
| Motion                   | Selected runtime package | Installed only as the normal npm runtime dependency; it is not an Agent Skill or MCP.               |
| GSAP official repository | Not selected             | It may be considered Essential only if GSAP is selected as the runtime; Motion is selected instead. |
| TypeUI                   | Rejected                 | Do not install, copy, or use as an implementation source.                                           |
| Taste                    | Optional                 | Manual design-review reference only; not installed.                                                 |
| MotionSites              | Reference-only           | Not installed and not used as a source of inaccessible implementation details.                      |

Any future manual installation requires explicit user approval and a separate security/license review.
